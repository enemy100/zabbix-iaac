---
- name: Reiniciar e habilitar serviços do Zabbix
  systemd:
    name: "{{ item }}"
    state: restarted
    enabled: yes
  loop:
    - zabbix-server
    - zabbix-agent
    - php-fpm
    - httpd

